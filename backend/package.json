{
  "name": "@cesargos/meu-drive",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "mkdir -p downloads && nodemon src/index.js",
    "prod": "mkdir -p downloads && NODE_ENV=production node src/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules npx jest --watch --runInBand",
    "test:cov": "NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand --no-cache --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "jest": "^30.2.0",
    "nodemon": "^3.1.11"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "pino": "^10.1.0",
    "pino-pretty": "^13.1.3",
    "pretty-bytes": "5.6",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": "20"
  }
}
